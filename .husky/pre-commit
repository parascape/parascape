#!/usr/bin/env node

const { execSync } = require('child_process');

try {
  // Check for merge conflict markers
  execSync('git diff --cached --check', { stdio: 'inherit' });
  
  // Run lint-staged
  execSync('npx lint-staged', { stdio: 'inherit' });
} catch (error) {
  process.exit(1);
} 